/*
        bRegister.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {


                if (TextUtils.isEmpty(password) || TextUtils.isEmpty(email)) {
                    Toast.makeText(
                            Register.this,
                            "Please fill all relevant fields",
                            Toast.LENGTH_LONG).show();
                }

                else {

                    try {

                        //if (isValidPassword(password)

                        if (password.length() > 0 && email.length() > 0) {

                            Toast.makeText(Register.this, "Valid Password Entered", Toast.LENGTH_LONG).show();
                            mAuth.createUserWithEmailAndPassword(email, password).addOnCompleteListener(new OnCompleteListener<AuthResult>() {
                                @Override
                                public void onComplete(@NonNull Task<AuthResult> task) {

                                    if (task.isSuccessful()) {

                                        sendEmailVerfication();
                                        // PD.dismiss();


                                    }

                                }
                            });
                        } else {
                            if (!isValidPassword(password) && password.length() > 0) {
                                Toast.makeText(Register.this, "Invalid Password", Toast.LENGTH_LONG).show();

                            }
                        }

                    } catch (Exception e) {
                        e.printStackTrace();
                    }
                }
            }
        });

        */ //end of reg button clicked






          mAuth = FirebaseAuth.getInstance();

                if (mAuth.getCurrentUser() != null) {
                    startActivity(new Intent(Register.this, home.class));
                    finish();
                }